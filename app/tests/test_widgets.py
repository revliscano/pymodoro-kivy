import unittest
# from app.GUI.widgets import TimerLabel


class TimerLabelTestCase(unittest.TestCase):
    def test_refresh(self):
        """
        self.timer_label = TimerLabel()
        self.timer_label.pomodoro_clock.run(2)
        self.timer_label.refresh()
        """
        self.assertEqual(1, 1)
